<resources>

    <style name="CursosPoint" parent="CursosPoint.Base" />

    <style name="CursosPoint.Base" parent="Theme.AppCompat.Light.NoActionBar">

        <item name="android:windowBackground">@color/window_background</item>

        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorAccent">@color/accentColor</item>
    </style>

    <style name="CursosPoint.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="CursosPoint.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
